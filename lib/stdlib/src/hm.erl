-module(hm).

-export([new/0, get/2, set/3, wk/0]).

new() -> erlang:nif_error(undefined).

get(_, _) -> erlang:nif_error(undefined).

set(_, _, _) -> erlang:nif_error(undefined).

wk() -> erlang:nif_error(undefined).
